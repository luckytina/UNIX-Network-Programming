5.7 正常终止
我们可以总结出正常终止客户和服务器的步骤。
（1）当我们键入EOF字符时，fgets返回一个空指针，于是str_cli函数返回。
（2）当str_cli返回到客户的main函数时，main通过调用exit终止。
（3）进程终止的处理的部分工作是关闭所有打开的描述符，因此客户打开的套接字
由内核关闭。这导致TCP发送一个FIN给服务器，服务器TCP以ACK响应，客户套接字
处于FIN_WAIT_2状态
（4）当服务器TCP接收FIN时，服务器进程阻塞于read调用，于是read返回为0.这导致str_echo函数返回服务器进程的main函数
（5）服务器子进程通过调用exit开终止
（6）服务器子进程中打开的所有描述符随之关闭。由子进程来关闭已连接的套接字会引发TCP连接终止序列的最后两个分节：一个
从服务器到客户的FIN和一个由客户到服务器的ACK。至此，连接完全终止，客户套接字进入TIME_WAIT状态。
（7）进程终止处理的另一部分是：在服务器子进程终止时，给父进程发送一个SIGCHLD的信号。

5.8 POSIX信号处理


5.9 处理SIGCHLD信号


